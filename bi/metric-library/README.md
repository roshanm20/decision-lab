# Metric library

One metric per file. Each file covers the formula, the edge cases that break it, how a team would game it, and the metric you pair it with to stop that.

Short files are fine here. Two hundred words that are correct beat a thousand that hedge.
